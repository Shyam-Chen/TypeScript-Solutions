{
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check --apply ./",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.3",
    "flatted": "^3.3.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
