{
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check --write ./",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.2",
    "flatted": "^3.3.1",
    "typescript": "^5.5.2",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
