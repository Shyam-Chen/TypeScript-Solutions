{
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check --write ./",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "flatted": "^3.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
