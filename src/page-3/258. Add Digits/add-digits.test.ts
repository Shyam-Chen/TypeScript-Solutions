import { addDigits, addDigits2, addDigits3, addDigits4, addDigits5 } from './add-digits';

describe('258. Add Digits', () => {
  test('addDigits', () => {
    expect(addDigits(38)).toBe(2);
    expect(addDigits(0)).toBe(0);
  });

  test('addDigits2', () => {
    expect(addDigits2(38)).toBe(2);
    expect(addDigits2(0)).toBe(0);
  });

  test('addDigits3', () => {
    expect(addDigits3(38)).toBe(2);
    expect(addDigits3(0)).toBe(0);
  });

  test('addDigits4', () => {
    expect(addDigits4(38)).toBe(2);
    expect(addDigits4(0)).toBe(0);
  });

  test('addDigits5', () => {
    expect(addDigits5(38)).toBe(2);
    expect(addDigits5(0)).toBe(0);
  });
});
